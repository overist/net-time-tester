// ** Type import
import { HorizontalNavItemsType } from 'src/@core/layouts/types'

const navigation = (): HorizontalNavItemsType => {
  // NOTE vertical 기반의 프로젝트이므로 사용하지 않습니다.
  return []
}

export default navigation
